$mainMod = SUPER

# Apps
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q, killactive
bind = $mainMod, M, exec, wlogout
bind = $mainMod, F, fullscreen
bind = $mainMod, N, exec, $fileManager
bind = $mainMod, D, exec, $menu
bind = $mainMod, Z, exec, $browser
bind = $mainMod, P, exec, grim -g "$(slurp)" - | wl-copy
bind = , Print, exec, grim ~/Pictures/Screenshots/$(date +%Y%m%d_%H%M%S).png
bind = $mainMod SHIFT, P, exec, grim -g "$(slurp)" ~/Pictures/Screenshots/$(date +%Y%m%d_%H%M%S).png
bind = $mainMod, Escape, exec, hyprlock

# Floating — like sway's $mod+Shift+Space / $mod+Space
bind = $mainMod SHIFT, Space, togglefloating
bind = $mainMod, Space, cyclenext

# Split direction toggle — like sway's $mod+b (splith/splitv)
bind = $mainMod, B, togglesplit

# Launch or toggle special workspaces
bind = $mainMod, T, exec, pgrep thunderbird && hyprctl dispatch togglespecialworkspace mail || thunderbird &
bind = $mainMod, S, exec, pgrep slack && hyprctl dispatch togglespecialworkspace slack || slack &

# Move focus — vim keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move focus — arrow keys
bind = $mainMod, Left,  movefocus, l
bind = $mainMod, Right, movefocus, r
bind = $mainMod, Up,    movefocus, u
bind = $mainMod, Down,  movefocus, d

# Move window — vim keys
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Move window — arrow keys (like sway's $mod+Shift+arrow)
bind = $mainMod SHIFT, Left,  movewindow, l
bind = $mainMod SHIFT, Right, movewindow, r
bind = $mainMod SHIFT, Up,    movewindow, u
bind = $mainMod SHIFT, Down,  movewindow, d

# Switch workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through workspaces
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up,   workspace, e-1

# Resize mode — like sway's $mod+r
bind = $mainMod, R, submap, resize
submap = resize
binde = , H,     resizeactive, -20 0
binde = , L,     resizeactive,  20 0
binde = , K,     resizeactive,  0 -20
binde = , J,     resizeactive,  0  20
binde = , Left,  resizeactive, -20 0
binde = , Right, resizeactive,  20 0
binde = , Up,    resizeactive,  0 -20
binde = , Down,  resizeactive,  0  20
bind  = , Return, submap, reset
bind  = , Escape, submap, reset
submap = reset

# Mouse move/resize
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
